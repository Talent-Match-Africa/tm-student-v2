/**
 * Value recorded for the event form's working place when a student reports
 * that they are not currently employed. The working place input is disabled in
 * that case, so this constant is the only value the portal submits for it.
 */
export const NOT_EMPLOYED_WORKING_PLACE = "Not Employed";
